name: CI

on: 
  push:

jobs:
  build:

    runs-on: macOS-latest
    
    steps:
    - uses: actions/checkout@master
    - name: Start xcodebuild test
      run: xcodebuild clean test -project BLT.xcodeproj -scheme TodoSampleApp -destination "platform=iOS Simulator,name=iPhone 8"
